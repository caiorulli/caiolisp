kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: haskell:latest
    commands:
      - cabal build

  - name: test
    image: haskell:latest
    commands:
      - cabal test --test-show-details=direct --test-option=--format=progress
